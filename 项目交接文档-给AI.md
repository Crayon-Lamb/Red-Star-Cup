# 红星杯时间线看板 — 项目交接文档

## 项目概述
第六届"红星杯"大学生创新创业大赛路演 — 时间逻辑顺序图，单文件 HTML 看板，包含时间线视图和部门分工视图，数据通过 Supabase 云端同步。

## 文件清单

| 文件 | 作用 |
|------|------|
| `红星杯时间线.html` | **主文件**，包含全部 HTML/CSS/JS |
| `index.html` | 跳转页 → 红星杯时间线.html |
| `server.py` | Flask 本地服务器（端口 5050，密码 `hongxingbei2026`） |
| `sort_timeline.py` | 排序工具脚本，按截止时间重排任务 |
| `交接文档.md` | 旧版交接文档（可忽略，以此文档为准） |

## 访问方式

| 方式 | 地址 |
|------|------|
| ☁️ GitHub Pages（推荐） | `https://crayon-lamb.github.io/Red-Star-Cup/` |
| 📁 本地 | `python -m http.server 8080` → `http://localhost:8080/红星杯时间线.html` |

- **访问密码**：`hxb2026`
- **管理员密码**（重置用）：`hxbadmin2026`

## 部署方式

任何对 `红星杯时间线.html` 的修改，提交并推送到 GitHub 即自动部署：

```bash
cd "C:/Users/28346/Desktop/测试"
git add "红星杯时间线.html"
git commit -m "描述你的改动"
git push
```

GitHub Pages 会在 1-2 分钟内自动更新。用户需 `Ctrl+Shift+R` 强制刷新查看最新版本。

## 如何修改任务

### 删除任务
找到对应的 `<div class="tl-item" data-phase="pX">` 整块（3-5行），直接删掉。

### 修改任务名称/时间
找到 `<span class="tl-name">` 或 `<span class="tl-date">`，修改文字即可。

### 添加任务
在对应阶段的合适位置（按时间排序），复制一份现有的 `tl-item` 块，修改名称、日期、部门。

### 任务块结构示例
```html
<div class="tl-item" data-phase="p1">
  <div class="tl-top"><span class="tl-name">任务名称</span><span class="tl-date">日期</span></div>
  <div class="tl-meta"><span class="tl-dept" style="...">部门</span></div>
  <div class="tl-note">备注（可选）</div>
</div>
```

## 页面结构

### 5 个阶段（tl-group）
| data-phase | 名称 | 日期范围 |
|------------|------|----------|
| p1 | 一、通知与报名 | 6月 — 9月中旬 |
| p2 | 二、路演相关准备 | 7月 — 10月初 |
| p3 | 三、路演当天 | 10月13日 |
| p4 | 四、初赛赛后总结 | 路演后 |
| p5 | 五、决赛 | 2027年4月 |

### p1 子阶段
- ▎6月 · 前期准备
- ▎7月 · 第一批预报名（除2026级）
- ▎8月 · 第二批预报名（2026级新生）
- ▎9月 · 正式报名 & 信息汇总

### p2 子阶段
- ▎7月 — 8月 · 活动设计与物料筹备
- ▎9月 · 宣传物料 & 场地准备
- ▎设计品清单
- ▎10月初 · 最后冲刺

### p3 子阶段
- ▎上午 · 签到与准备
- ▎中午 · 路演正式开始

### p4 子阶段
- ▎计票工作
- ▎场地清理
- ▎总结与宣传

### p5 子阶段
- 决赛（无子阶段标题，直接一个任务）

## 部门与工作组

### 9 个部门（p1/p2 默认使用）
学术实践部、办公室、组织部、宣传部、文艺部、生活权益部、信息技术部、研究生办公室、体育部

### 6 个工作组（p3 默认使用，p4/p5 也可选）
票务服务组、现场秩序组、互动引导组、机动应急组、摄像组、工作组

## 日期格式参考（用于排序）
- 精确日期：`6月19日`、`10月9日`
- 修饰日期：`6月30日前`、`10月6日前后`、`9月15日晚`
- 模糊日期：`6月下旬`、`7月上旬`、`8月底`、`9月初`
- 范围：`9月1日 — 9月8日`、`7月20日 — 21日晚`
- 特殊：`路演全程`、`路演后`、`路演结束后`、`(持续进行)`、`暑假期间`
- 时间点：`10:30`、`12:00`

## Supabase 云端同步

- 项目 ID：`wgfdezkdgnnygprdaics`
- 匿名 Key：已内嵌在 HTML 中（`SUPABASE_KEY` 常量）
- 表名：`tasks`
- 同步机制：填写负责人后 400ms 自动上传；页面加载时拉取；每 10 秒轮询

## 工具栏按钮
🔄 同步 | ⬆️ 上传 | 📥 导出 | 📤 导入 | 🗑️ 重置全部 | 📅/👥 视图切换

## 注意事项
1. 不要在本地 `file://` 打开，同步功能需要 GitHub Pages
2. 部门视图自动从时间线 DOM 读取，修改时间线后部门视图自动同步
3. p1 用 9 个部门，p3 用 6 个工作组，p4/p5 两者都可用
4. 每次部署后提醒用户 Ctrl+Shift+R 强制刷新
5. 如果改动涉及任务增删，部门视图会自动反映
